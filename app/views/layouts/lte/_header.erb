<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#">
                <span class="icon" data-toggle-tooltip-bottom="tooltip" data-title="Свернуть/Развернуть меню"><%= icon('fas', 'bars') %></span>
            </a>
        </li>
    </ul>


    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto user-navbar-menu">
        <li class="nav-item user-menu">
            <a href="#" class="nav-link" data-toggle="dropdown">
                <i class="fas fa-user"></i>
                <span class="d-none d-md-inline">&nbsp;<%= current_user.email || 'Пользователь' %></span>
            </a>
            <div class="dropdown-menu dropdown-menu-right">
                <span class="dropdown-header" style="text-align:left;white-space:normal;">
                    <span><b>Логин:</b> <%#= current_user.login %></span><br>
                    <span><b>Роль:</b> <%#= current_user.role.label %></span><br>
                </span>
                <div class="dropdown-divider"></div>
                <span class="dropdown-item" style="padding-bottom: 10px;">
                    <div class="row">
                        <div class="col">
                            <%#= lte_button_to({text: 'Профиль', icon: 'fa-address-card', path: user_path(current_user), class: 'btn-primary btn-block btn-sm'}) %>
                        </div>
                        <div class="col">
                            <%= lte_button_to({text: 'Выйти', icon: 'fa-sign-out-alt', path: destroy_user_session_path, method: 'delete', class: 'btn-warning btn-block btn-sm'}) %>
                        </div>
                    </div>
                </span>
            </div>
        </li>
    </ul>
</nav>
<!-- /.navbar -->
